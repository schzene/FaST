#include "ffn.h"

void FFN::forward(const LongCiphertext &ln1) {
    if (party->party == ALICE) {
#ifdef LOG
        std::cout << "Secure Feed Forward done.\n";
#endif
    } else {
#ifdef LOG
        std::cout << "Secure Feed Forward done.\n";
#endif
    }
}